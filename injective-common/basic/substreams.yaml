specVersion: v0.1.0
package:
  name: injective_usdt_volume
  version: v0.1.0

network: cosmos

protobuf:
  descriptorSets:
    - module: buf.build/cosmos/cosmos-sdk
      version: v0.50.0
      symbols:
        - cosmos.tx.v1beta1.Tx
    - module: buf.build/streamingfast/firehose-cosmos
  files:
    - cosmos/v1/tx_list.proto
  importPaths:
    - ./proto

binaries:
  default:
    type: wasm/rust-v1
    file: target/wasm32-unknown-unknown/release/injective_usdt_volume.wasm

modules:
  - name: map_usdt_exchanges
    kind: map
    initialBlock: 64987400
    inputs:
      - source: sf.cosmos.type.v2.Block
    output:
      type: proto:sf.substreams.cosmos.v1.USDTExchangeList
